{
  "name": "project-bushido",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "echo 'No ESLint configured yet'",
    "typecheck": "npx tsc --noEmit",
    "build:check": "npm run typecheck && npm run build",
    "deploy": "npx vercel --prod"
  },
  "dependencies": {
    "simplex-noise": "^4.0.1",
    "three": "^0.160.0"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^5.0.12",
    "vite-plugin-checker": "^0.14.5"
  }
}
